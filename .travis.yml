language: objective-c
osx_image: xcode10.2

before_script:
    - carthage bootstrap

script:
    # Build Travis project and run tests
    - xcodebuild -project ShareClient.xcodeproj -scheme Shared build -destination name="iPhone SE" test 
